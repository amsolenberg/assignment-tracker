services:
  assignment-tracker:
    build: .
    container_name: assignment-tracker
    restart: unless-stopped

    ports: 
      - "3000:3000"

    volumes:
      - ./data:/app/data

    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_PATH=/app/data/assignments.db